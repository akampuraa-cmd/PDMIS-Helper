# PDMIS Helper — Python dependencies
# Install with:  pip install -r requirements.txt
# Then run:      playwright install chromium

playwright>=1.44.0
pandas>=2.0.0
openpyxl>=3.1.0        # required by pandas for .xlsx export
